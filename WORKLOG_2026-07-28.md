# 对账系统工作纪要（2026-07-28）

## 当前线上版本

- GitHub：`git@github.com:wangmiao033/cf.hnchpower.cn.git`
- 生产分支：`main`
- 本次功能提交：`b3cfa97 feat: make channel reconciliation progress actionable`
- Vercel 项目：`cf-hnchpower-cn`
- 正式域名：`https://cf.hnchpower.cn`
- Vercel 域名：`https://cf-hnchpower-cn.vercel.app`

## 本次完成内容

### 渠道对账进度

- 待处理流水增加“开始核对”和“重新核对”入口。
- 新增核对弹窗，可搜索并选择渠道账单。
- 默认只展示同月渠道账单，避免跨月误匹配。
- 历史账单可单独查看，但不能直接跨月确认。
- 当前月份没有账单时，可直接新增渠道账单或查看历史账单。
- 完全匹配后从待处理列表移除，并更新已核对、待处理和金额统计。
- 部分匹配时保留待处理记录，并显示剩余差额。
- 核对结果保存在浏览器本地数据中。

### 质量检查

- 单元测试：`69` 项全部通过。
- 生产构建：通过。
- `git diff --check`：通过。
- Vercel 生产部署：`Ready`。
- 已确认生产别名包含 `cf.hnchpower.cn` 和 `cf-hnchpower-cn.vercel.app`。

## 关键文件

- `src/components/channel/ChannelReconciliationProgressPanel.jsx`
  - 渠道核对弹窗、候选账单、同月限制、确认操作。
- `src/domain/channel/channelReconciliationProgress.js`
  - 渠道核对进度计算及匹配结果更新。
- `src/domain/channel/channelReconciliationMatch.test.js`
  - 完全匹配和部分匹配测试。
- `src/pages/RdReconciliationProgressPage.jsx`
  - 游戏/渠道进度页面的数据接入及持久化。
- `src/pages/CoreReconciliationPages.css`
  - 对账弹窗和响应式样式。

## 回家后在 Mac 继续

```bash
git clone git@github.com:wangmiao033/cf.hnchpower.cn.git
cd cf.hnchpower.cn
git checkout main
npm install
npm run dev
```

若仓库已存在：

```bash
cd cf.hnchpower.cn
git checkout main
git pull origin main
npm install
npm run dev
```

本地地址以终端输出为准，通常是 `http://localhost:3000` 或 `http://localhost:3001`。

## 建议下一步

1. 用真实的渠道账单完整走一次“同月匹配、部分匹配、完全匹配”流程。
2. 明确渠道核对结果是否只保存在浏览器，还是写入云端数据库并支持多人共享。
3. 为核对操作增加操作日志，包括操作人、时间、来源流水、匹配账单和差额。
4. 将“已核对”“已登记应收”“已回款”拆成明确的业务状态，避免统计口径混用。
5. 对正式域名做登录、研发账单、渠道账单、对账进度、数据库、合同台账和客户库的完整回归测试。

## 注意事项

- 当前核对结果主要依赖浏览器本地持久化；换电脑后不会自动带走本地未同步的数据。
- 正式业务数据应逐步迁移到云端数据库，避免不同电脑之间出现数据不一致。
- 不要在仓库或工作纪要中保存账号密码、Token、数据库密钥等敏感信息。
